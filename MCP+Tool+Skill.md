其实它们三个都存在，那必有其原因。虽然我觉得很多东西，像 MCP 跟 Tool 之间都已经融合了，但为了方便记忆，可以这么理解：

Tool calling 概念上是包括了本地直接工具（即函数直接在应用里面写好了，现场LLM response 点名要哪个就直接调用得了。) 和 MCP 的（虽然MCP也可以写成本地的， 本地MCP和tool calling作用上区别不大）。

而 MCP 这边可以写本地的，也可以写远程的。MCP 存在的意义其实就在于远程，一旦支持远程，自然就可以直接用其他 provider 提供的 MCP。比如像 Gmail、Notion、Google Drive 这些不开源的工具，让人家开源属实是在说梦话；当然也不可能让你在本地把这些 function 再自己实现一遍，那纯属胡闹，所以这个时候就直接用人家的 MCP。

这种 MCP 本质上就是在工具原本提供的 API 外面再包一层，让 Large Language Model 知道怎么去拼接 request / 照着已经定义好的半成品request加变量post出去 ，或者直接把一套成熟的流程告诉模型：比如要更新就去 POST 这个 request，要读什么就去干嘛，要删什么就去干嘛。其实就是给它选项，再加上一些必要的拼接，起码用户这边的各种 header、authentication 之类的，该拼的还是得拼。

Skill 这边的话，其实可以理解成是懒得每次都复制粘贴一遍的那种固定、好使的 prompt。每次如果要给大模型那边发这种步骤，每次粘贴不累得慌吗？自己又记不住，还得找个文档保存粘贴，很不优雅，所以就搞成了 Skill。

现在 Skill 那边其实跟 MCP 差不多，都不至于直接把原文全暴露过去，不然 context 得塞爆。

现在的做法大家其实都一样，都是先只给目录。大模型那边看了目录，说它需要什么，然后再把对应 Skill 完整的原文输入给模型。

-这个其实就是 context window 有限这件事情，贯穿了 Large Language Model 的始终。不管是大家一开始搞 context engineering、prompt engineering，还是搞出来的 MCP、tool calling，以及不把全部内容一次性塞给模型、只先把目录给它，或者做 compacting，核心其实都一样，都是因为受限于有限的 context window。

再往下扒，为什么模型没办法去处理这么长的东西？当我们说它没办法处理这么长的时候，并不是说往里面塞不下，而是指在这么大一段 input 里面，使用者真正想要的那几个核心目标点，会随着输入内容不断增多，逐渐被模型给淡忘掉。

Language Model 毕竟不是人，它对待所有 input 往往还蛮“一视同仁”的。虽然技术上可以在计算中做到一些加权或强调（emphasize），但当输入源源不断推给它的时候，模型还是会遗忘掉一些点。而这些点在大多数时候，恰恰是用户真正想要达成的目标，模型却给忘了。

其实再往下一层剖析，就涉及到 Transformer 架构里 attention 机制本身的一些底层问题了。反正简而言之，就是当前世界上最聪明的一帮聪明人的脑子，他们能达到的最好的，by far 就是当前这个样子的了。

那就接受限制，然后在这个限制中去给他搞出花来。

接着谈，因为有 context window 的一些限制，所以得用时间去换空间。欸，这怎么又跟算法那边连上了，反正就两个选择：要么空间换时间，要么时间换空间。

我们这边选的是用时间换空间。context window 是空间，空间有限，不能什么都往里面塞，所以只能多来几个 turn、多来几轮 iteration，只选 large language model 那边决定要的东西塞到 context window 里面，不要的就别往里塞，就是这么一个逻辑。

而这其中反复来回，别管是tool calling, skills 还是 本地/远程 MCP，它中间都会有很多轮反复的 turn，即为请求次数换空间。
